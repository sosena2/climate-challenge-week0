# Notebooks

This folder contains exploratory analysis notebooks for each country dataset.

Current notebooks:
- `ethiopia_eda.ipynb`
- `kenya_eda.ipynb`

Each notebook should read its matching raw CSV from the repository root and export the cleaned result to `data/<country>_clean.csv`.
